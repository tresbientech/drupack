*
!drupal/composer.json
!drupal/composer.lock
!runtime/
!runtime/**
# A local composer install symlinked here for tests that run outside the
# build (launch.php now requires vendor/autoload.php); the image builds its
# own vendor/ from drupal/composer.json and would collide with this one.
runtime/vendor
!packaging/
!packaging/**
!Dockerfile
