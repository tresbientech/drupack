# The libraries a builder names to static-php-cli beside the extension
# allowlist. No extension pulls them: watcher serves FrankenPHP, the nghttp
# trio serves Caddy, and libavif adds AVIF to gd.

libavif
nghttp2
nghttp3
ngtcp2
watcher
